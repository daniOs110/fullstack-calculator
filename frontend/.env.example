# Base URL of the Go API (no trailing slash).
#
# Local dev (Vite on :5173, Go on :8080):
#   VITE_API_BASE_URL=http://localhost:8080
#
# Docker Compose (nginx proxies /api to the backend on the same origin):
#   VITE_API_BASE_URL=
VITE_API_BASE_URL=http://localhost:8080
